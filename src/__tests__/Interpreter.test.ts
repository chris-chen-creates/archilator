import {
  Addition,
  Multiplication,
  Negative,
  Group,
  Num,
  Division,
} from '../Expression'
import { Interpreter } from '../Interpreter'
import { scan } from '../Scanner'

// test('addition works correctly', () => {
//   const eval = new Interpreter(scan('5 + 5'))
// })
